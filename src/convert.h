#ifndef _CONVERT_H_
#define _CONVERT_H_

#include <iostream>
#include <sstream>
#include <bitset>
#include <map>
#include <vector>

using namespace std;

string hextobin(const string&);

int bintoint(const string&);

vector<string> getinstr(const string&);

#endif
